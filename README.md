# Face_Mask_Detector

A project for detecting the presence of a mask on a person's face.

## Installation
1. Clone the repository
2. Install dependencies: `pip install -r requirements.txt`
3. Download the dataset to the `dataset/` folder
